apiVersion: v1
kind: Service
metadata:
  name: {{ include "crewai.fullname" . }}
  labels:
    {{- include "crewai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "crewai.selectorLabels" . | nindent 4 }}
---
