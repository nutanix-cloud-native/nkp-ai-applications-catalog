drafts-repo dir is a valid catalog repo that hosts apps that are not yet oci compatible.
untested-repo dir is a valid catalog repo that hosts apps that pass manifest validation but are not validated in e2e yet

we can admit apps from parking-lot into top level applications as we validate apps one by one.
