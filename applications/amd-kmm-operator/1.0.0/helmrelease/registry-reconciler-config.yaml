apiVersion: v1
kind: Secret
metadata:
  name: amd-kmm-operator-registry-reconciler-config
  namespace: ${releaseNamespace}
type: Opaque
stringData:
  CREDENTIALS_SECRET: "${credentialsSecretName}"
  DOCKERCONFIG_SECRET: "${dockerconfigSecretName}"
  DAEMONSET_NAME: "${daemonsetName}"
