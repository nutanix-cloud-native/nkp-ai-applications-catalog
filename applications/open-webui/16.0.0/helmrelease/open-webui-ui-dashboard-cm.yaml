apiVersion: v1
kind: ConfigMap
metadata:
  name: ${releaseName}-ui
  namespace: ${workspaceNamespace}
  labels:
    "kommander.d2iq.io/application": ${releaseName}
  annotations:
    "kustomize.toolkit.fluxcd.io/ssa": "Merge"
data:
  name: "Open WebUI"
  docsLink: "https://docs.openwebui.com/"
  version: "16.0.0"
  # dashboardLink is patched by the postinstall Job to https://<ingress.host>/
---

