apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ${releaseNamespace}
configMapGenerator:
  - name: grafana-dashboards-amd-network-operator
    files:
      - dashboard_ainic_system.json
      - dashboard_ainic_job.json
generatorOptions:
  labels:
    grafana_dashboard: "1"
