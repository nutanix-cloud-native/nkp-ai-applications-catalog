apiVersion: v1
kind: ConfigMap
metadata:
  name: kserve-0.16.0-nkp-defaults
  namespace: ${releaseNamespace}
data:
  values.yaml: |-
    kserve:
      controller:
        deploymentMode: RawDeployment
      gateway:
        disableIngressCreation: true
---
