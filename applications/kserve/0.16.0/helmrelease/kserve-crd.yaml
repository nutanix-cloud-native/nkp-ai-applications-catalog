apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: ${releaseName}-crd-0.16.0-chart
  namespace: ${releaseNamespace}
spec:
  url: oci://ghcr.io/kserve/charts/kserve-crd
  ref:
    tag: v0.16.0
  interval: 10m
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kserve-crd
  namespace: ${releaseNamespace}
spec:
  chartRef:
    kind: OCIRepository
    name: ${releaseName}-crd-0.16.0-chart
    namespace: ${releaseNamespace}
  interval: 15s
  install:
    crds: CreateReplace
    createNamespace: true
    remediation:
      retries: 30
  upgrade:
    crds: CreateReplace
    remediation:
      retries: 30
  releaseName: kserve-crd
  targetNamespace: kserve
---