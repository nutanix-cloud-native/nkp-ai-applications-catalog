apiVersion: v1
data:
  values.yaml: |
    fullnameOverride: "crewai"
    persistence:
      enabled: true
      size: 10Gi
    resources:
      limits:
        cpu: 2000m
        memory: 4Gi
      requests:
        cpu: 500m
        memory: 2Gi
kind: ConfigMap
metadata:
  name: ${releaseName}-1.0.0-defaults
  namespace: ${releaseNamespace}
---
