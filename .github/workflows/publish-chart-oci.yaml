# Copyright 2025 Nutanix. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

name: Publish Chart to GHCR

on:
  workflow_dispatch:
    inputs:
      chart_repo:
        description: 'Source Helm/OCI repository URL (e.g., https://weaviate.github.io/weaviate-helm/, oci://harbor.eng.nutanix.com/amd-operators)'
        type: string
        required: true
      repo_name:
        description: 'Helm repo alias for helm repo add / helm pull (e.g., weaviate, ollama-helm)'
        type: string
        required: true
      chart_name:
        description: 'Helm chart name in the repo (e.g., weaviate, ollama)'
        type: string
        required: true
      chart_version:
        description: 'Helm chart version'
        type: string
        required: true
      target_oci_registry:
        description: 'Target OCI registry (e.g., oci://ghcr.io/nutanix-cloud-native/charts)'
        type: string
        required: false
        default: 'oci://ghcr.io/nutanix-cloud-native/charts'

jobs:
  push-chart:
    name: Publish Chart to GHCR
    runs-on:
      - self-hosted-nutanix-medium
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Nix on self-hosted ARC runners
        uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install devbox
        uses: jetify-com/devbox-install-action@v0.13.0
        with:
          skip-nix-installation: true

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push chart using just/script
        run: |
          devbox run -- just push-helm-to-oci \
            "${{ github.event.inputs.chart_name }}" \
            "${{ github.event.inputs.repo_name }}" \
            "${{ github.event.inputs.chart_repo }}" \
            "${{ github.event.inputs.chart_name }}" \
            "${{ github.event.inputs.chart_version }}" \
            "${{ github.event.inputs.target_oci_registry }}"
        env:
          GITHUB_USERNAME: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
